<!-- kmark page_number:hide -->

# ページ番号テスト 表紙

このページは表紙です。

期待:
- 表紙にはページ番号を表示しない
- 次の本文ページから `1` として開始する

<!-- --- -->

<!-- kmark page_number:show page_number_start:1 page_number_format:"{page}" page_number_position:bottom_center -->

# 1. 本文開始ページ

このページからページ番号を表示します。

期待:
- ページ下中央に `1` と表示される
- 表紙はページ番号カウントに含めない

## 通常文章

これはページ番号表示の基本確認用文章です。

- 箇条書きがあってもページ番号が表示される
- ページ下部の本文と重ならない
- 印刷時にも同じ位置に表示される

<!-- --- -->

# 2. 2ページ目

期待:
- ページ番号が `2` と表示される
- 前ページの設定が継続される

## 表の確認

| No | 項目 | 内容 |
|---:|---|---|
| 1 | page number | ページ番号表示 |
| 2 | position | 下中央 |
| 3 | format | `{page}` |
| 4 | print | 印刷確認 |

<!-- --- -->

<!-- kmark page_number_format:"Page {page} / {total}" page_number_position:bottom_right -->

# 3. 表示形式変更

このページからページ番号の表示形式を変更します。

期待:
- 右下に `Page 3 / 総ページ数` の形式で表示される
- 以降のページにもこの形式が継続される

## 確認内容

- `{page}` が現在ページ番号に置換される
- `{total}` が総ページ数に置換される
- 右下配置になる

<!-- --- -->

# 4. 形式継続確認

このページではページ番号設定を書いていません。

期待:
- 前ページの設定が継続される
- 右下に `Page 4 / 総ページ数` が表示される

## 長めの文章

ここでは本文量が多い場合に、ページ番号が本文と重ならないことを確認します。

これはテスト用の文章です。kmarkではMarkdownから直接ページプレビューを生成し、ページ単位で表示や印刷を行います。ページ番号は本文とは独立したページ装飾として扱われるべきです。

これはテスト用の文章です。kmarkではMarkdownから直接ページプレビューを生成し、ページ単位で表示や印刷を行います。ページ番号は本文とは独立したページ装飾として扱われるべきです。

これはテスト用の文章です。kmarkではMarkdownから直接ページプレビューを生成し、ページ単位で表示や印刷を行います。ページ番号は本文とは独立したページ装飾として扱われるべきです。

<!-- --- -->

<!-- kmark page_number_position:top_center page_number_format:"- {page} -" -->

# 5. 上中央表示

このページからページ番号を上中央に表示します。

期待:
- ページ上中央に `- 5 -` と表示される
- 見出しと重ならない
- 印刷時にも上中央に表示される

## 確認用

ページ番号を下部だけでなく上部にも表示できるか確認します。

<!-- --- -->

<!-- kmark page_number:hide -->

# 6. ページ番号非表示ページ

このページではページ番号を非表示にします。

期待:
- このページにはページ番号が表示されない
- 次ページで再度表示できる

このようなページは、区切りページ・白紙ページ・章扉などで使う想定です。

<!-- --- -->

<!-- kmark page_number:show page_number_position:bottom_center page_number_format:"{page}" -->

# 7. 再表示ページ

このページではページ番号を再表示します。

期待:
- ページ番号が再び表示される
- カウント自体は継続される
- 表示だけを一時的にOFF/ONできる

## コードブロック確認

```c
#include <stdio.h>

int main(void)
{
    printf("kmark page number test\n");
    return 0;
}
```

<!-- --- -->

# 8. 自動ページ割確認用

このページは文章量を多めにして、自動ページ割が発生する場合の確認に使います。

期待:
- 自動で次ページに分割されてもページ番号が正しく増える
- 途中で生成されたページにもページ番号が表示される
- 本文とページ番号が重ならない

## 長文

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

kmarkのページ番号は、Markdown本文の一部ではなく、ページ表示の装飾として扱う。  
そのため、本文の段落、表、画像、コードブロック、手動ページ割、自動ページ割に影響されず、各ページの決められた位置に安定して表示される必要がある。

<!-- --- -->

<!-- kmark page_number_format:"p.{page}" page_number_position:bottom_left -->

# 9. 左下表示

このページではページ番号を左下に表示します。

期待:
- 左下に `p.9` の形式で表示される
- 下中央や右下の表示が残らない
- ページ番号設定が正しく上書きされる

<!-- --- -->

# 10. 最終ページ

最終確認ページです。

期待:
- ページ番号が表示される
- 総ページ数を使っているページでは、最終ページまで含めた総数が入る
- プレビューと印刷で番号・位置・表示形式が一致する

## 確認項目まとめ

- 表紙は非表示
- 本文開始ページを `1` にできる
- ページ番号表示を途中で変更できる
- `{page}` が使える
- `{total}` が使える
- 上下左右の配置を確認できる
- 一時的に非表示にできる
- 自動ページ割でも番号が崩れない

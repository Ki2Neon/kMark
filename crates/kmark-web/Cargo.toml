[package]
name = "kmark-web"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kmark-core = { path = "../kmark-core" }
kmark-contract = { path = "../kmark-contract" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasm-bindgen = "0.2"

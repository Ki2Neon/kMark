{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview-window",
  "description": "Capability for the detached preview window",
  "windows": ["preview-window"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title"
  ]
}
